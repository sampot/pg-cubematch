import{newGame,flip,collapse,score}from"./game.js";let s=newGame(),busy=false;const pets=["cat","dog","fox","panda","pig","bunny"];const G=document.querySelector("#game"),H=document.querySelector("#hud"),M=document.querySelector("#message"),C=document.querySelector("#controls");C.innerHTML='<button id="again">重新洗牌</button>';
function render(){H.innerHTML=`<div>步數<b>${s.moves}</b></div><div>配對<b>${s.matched.length/2}/6</b></div><div>分數<b>${score(s)??"—"}</b></div>`;G.innerHTML=`<div class="grid" style="grid-template-columns:repeat(3,1fr);padding:10px">${s.cards.map((p,i)=>{const up=s.open.includes(i)||s.matched.includes(i);return`<button data-i="${i}" style="aspect-ratio:1;background:${up?'#e7f0ff':'#354464'};padding:6px" aria-label="${up?pets[p]:'蓋牌'}">${up?`<img src="assets/animal-${pets[p]}.png" alt="${pets[p]}" style="width:100%;height:100%;object-fit:contain">`:"❔"}</button>`}).join("")}</div><div class="overlay" ${s.over?"":"hidden"}><div class="panel"><h2>全部配對！</h2><p>${s.moves} 步 · ${score(s)} 分</p><button id="winAgain">再玩一次</button></div></div>`;G.querySelectorAll("[data-i]").forEach(b=>b.onclick=()=>pick(+b.dataset.i));G.querySelector("#winAgain")?.addEventListener("click",reset);if(s.over)fetch("/api/kv/pg-cubematch-best",{method:"PUT",body:String(score(s))}).catch(()=>{})}
function pick(i){if(busy)return;const r=flip(s,i);s=r.state;render();if(r.event==="miss"){busy=true;M.textContent="不一樣，再記一下";setTimeout(()=>{s=collapse(s);busy=false;M.textContent="找出相同寵物";render()},700)}else if(r.event==="match")M.textContent="配對成功！"}function reset(){s=newGame();busy=false;M.textContent="找出相同寵物";render()}C.querySelector("#again").onclick=reset;reset();
